# ===========================================
# Better Coaching - Production Environment
# ===========================================
# Copy this to .env and fill in your values

# Database password (used by docker-compose)
DB_PASSWORD=your-secure-database-password

# JWT Secrets - Generate with: openssl rand -hex 64 (64 chars for security)
JWT_SECRET=your-jwt-secret-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here

# Encryption Key - Generate with: openssl rand -hex 32 (REQUIRED)
ENCRYPTION_KEY=your-encryption-key-here

# CORS (comma-separated allowed origins)
CORS_ORIGIN=https://bettercoachingapp.com

# AI Model API Keys (at least one required)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=

# Stripe (for creator subscriptions)
STRIPE_SECRET_KEY=sk_live_...

# RevenueCat (for coach purchases)
# Get from RevenueCat Dashboard > Project Settings > API Keys > Secret API Key
REVENUECAT_SECRET_KEY=
REVENUECAT_WEBHOOK_SECRET=

# Creator webhook secret (shared between web app and backend)
# Generate with: openssl rand -hex 32
CREATOR_WEBHOOK_SECRET=
