# ===========================================
# Better Coaching Backend Environment Variables
# ===========================================

# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# For local development:
DATABASE_URL="postgresql://postgres:password@localhost:5432/bettercoaching"
# For Docker Compose:
# DATABASE_URL="postgresql://bettercoaching_user:changeme@db:5432/bettercoaching_app"

# JWT Authentication
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-change-in-production
JWT_REFRESH_SECRET=your-jwt-refresh-secret-change-in-production

# CORS (include web app origin for creator portal)
CORS_ORIGIN="http://localhost:8081,http://localhost:3001"

# Web App URL (for OAuth redirects)
WEB_APP_URL=http://localhost:3001

# AI Model API Keys (at least one required)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# RevenueCat (optional - for subscriptions)
REVENUECAT_WEBHOOK_SECRET=

# ===========================================
# Knowledge Sources / Integrations
# ===========================================

# Encryption key for OAuth tokens (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-change-in-production

# Notion OAuth (https://www.notion.so/my-integrations)
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=
NOTION_REDIRECT_URI=http://localhost:3000/api/integrations/oauth/notion/callback

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/integrations/oauth/google/callback

# ===========================================
# Setup Instructions:
# ===========================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your values (generate JWT secrets!)
# 3. Generate Prisma client: npm run db:generate
# 4. Push schema to database: npm run db:push
# 5. Seed database: npm run db:seed
# 6. Start dev server: npm run dev
#
# For Docker deployment:
# 1. Copy ../.env.production.example to ../.env
# 2. Fill in production values
# 3. Run: docker-compose up -d
